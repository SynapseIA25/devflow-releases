// Carpeta de proyecto por defecto (semilla). La carpeta activa real vive en projectStore.ts
// (persistida) y se puede cambiar en vivo con el selector de carpeta del File Explorer.
// Este valor solo se usa como valor inicial la primera vez, antes de que el usuario elija otra.
export const PROJECT_CWD = "F:\\mimo-agent";
